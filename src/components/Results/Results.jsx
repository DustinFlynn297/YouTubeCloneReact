import React from 'react';

const Results = (props) => {
    return (
                <tr>
                    <td>{props.result.id.videoId}</td>
                </tr>
    );
}

export default Results;